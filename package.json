{
  "name": "wc-results",
  "version": "0.1.0",
  "private": true,
  "description": "Internal World Cup 2026 prediction game (points only, no real betting).",
  "scripts": {
    "dev": "wrangler pages dev --local",
    "fixtures": "node scripts/generate-fixtures.mjs",
    "db:init": "wrangler d1 execute wc-results --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute wc-results --remote --file=./schema.sql",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
